{
  "name": "playwright_ps_demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf screenshots test-results playwright-report",
    "test": "npm run clean && playwright test",
    "debug": "npm run clean && playwright test --debug",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@faker-js/faker": "^10.5.0",
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}