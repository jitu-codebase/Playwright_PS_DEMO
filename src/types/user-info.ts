/* TypeScript type definition for user checkout data, containing firstName, lastName, and postalCode fields */
export type userData = {
    firstName: string,
    lastName: string,
    postalCode: string
}